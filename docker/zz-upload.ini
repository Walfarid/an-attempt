; Production upload limits.
; The server-side validation accepts up to 4 MB (max:4096), so PHP's limits
; must be at least that high. Set to 10 MB for headroom.
upload_max_filesize = 10M
post_max_size = 12M
